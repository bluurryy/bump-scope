[package]
name = "fuzzing-support"
version = "0.0.0"
edition = "2024"

[dependencies]
arbitrary = { version = "1.4.2", features = ["derive"] }
bump-scope = { path = "../.." }
log = "0.4.34"
rangemap = "1.8.0"
zerocopy = { version = "0.8.56", features = ["derive"] }
