[package]
name = "tests-from-std"
edition = "2024"

[dev-dependencies]
bump-scope = { path = "../..", features = [
  "nightly-allocator-api",
  "nightly-coerce-unsized",
  "nightly-dropck-eyepatch",
] }
